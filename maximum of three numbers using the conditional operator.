#include <stdio.h>
 
int main() {
  	int a, b, c, max;
  	printf("25331A05G3\n");
  	printf("Enter three numbers: ");
  	scanf("%d %d %d", &a, &b, &c);
  	max = (a > b) ? ((a > c) ? a : c) : ((b > c) ? b : c);
  	printf("Maximum = %d\n", max);
  	return 0;
}
